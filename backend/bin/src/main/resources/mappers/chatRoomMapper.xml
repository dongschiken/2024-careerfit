<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.peach.careerfit.chat.model.dao.ChatRoomMapper">
<!-- 채팅방 생성 -->
<insert id="insertChatRoom" parameterType="string">
    INSERT INTO chat_room (title, last_at, created_at, updated_at)
    VALUES (#{title}, NOW(), NOW(), NOW())
</insert>

<!-- 채팅방 삭제 -->
<delete id="deleteChatRoom" parameterType="int">
	DELETE FROM chat_room WHERE chat_room_id = #{chat_room_id}
</delete>

<!-- 채팅방에 유저 추가 -->
<insert id="insertChatRoomUser">
	INSERT INTO chat_room_user (chat_room_id, user_id, joined_at)
    VALUES (#{chatRoomId}, #{userId}, NOW())
</insert>

<!-- 유저가 이미 채팅방에 있는지 확인 -->
<select id="countChatRoomUser" resultType="int">
     SELECT COUNT(*) FROM chat_room_user 
     WHERE chat_room_id = #{chatRoomId} AND user_id = #{userId}
</select> 

<!-- 채팅방 나가기 -->
<delete id="deleteChatRoomUser">
	DELETE FROM chat_room_user
	WHERE chat_room_id = #{chatRoomId} AND user_id = #{userId}
</delete>

<!-- 채팅방 회원 목록 조회 -->
<select id="selectParticipantsByChatRoom" resultType="ChatParticipantResponse">
	SELECT user_id, joined_at
	FROM chat_room_user
	WHERE chat_room_id = #{chatRoomId}
	LIMIT #{size} OFFSET #{offset}
</select>
</mapper>