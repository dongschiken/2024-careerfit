<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.peach.careerfit.user.model.dao.UserMapper">
    <!-- memberId로 사용자 조회 -->
    <select id="findByUserEmail" parameterType="String" resultType="com.peach.careerfit.user.model.dto.User">
        SELECT * 
        from user
        WHERE email = #{email}
    </select>
	<insert id="insertUser" parameterType="User">
		INSERT INTO user (
		    email,
		    password,
		    name,
		    nickname,
		    phone,
		    post_code,
		    parcel_address,
		    street_address,
		    latitude,
		    longitude,
		    created_at,
		    updated_at,
		    role,
		    status
		) VALUES (
		    #{email}, #{password}, #{name}, #{nickname}, #{phone}, #{postCode}, #{parcelAddress}
		    ,#{streetAddress}, #{latitude}, #{longitude}, #{createdAt}, #{updatedAt}, #{role}, #{status}
		)
	</insert>
</mapper>
