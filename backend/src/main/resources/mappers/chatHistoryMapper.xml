<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.peach.careerfit.chat.model.dao.ChatHistoryMapper">
	<!-- 메세지 삽입 -->
	<insert id="insertMessage">
		INSERT INTO chat_history(chat_room_id, send_user_id, message, send_date)
		VALUES (#{chatRoomId}, #{sendUserId}, #{message}, CURRENT_TIMESTAMP)
	</insert>	
	
	<!-- 채팅방의 메세지 목록 조회 -->
	 <select id="getMessagesByChatRoomId" resultType="ChatMessageResponse">
        SELECT chat_room_id, send_user_id, message, send_date
        FROM chat_history
        WHERE chat_room_id = #{chatRoomId}
        ORDER BY send_date ASC
    </select>
</mapper>